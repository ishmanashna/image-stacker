<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="ImageStacker.App.CustomColorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom color"
        Width="360"
        Height="160"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Enter a color name (e.g. white) or hex (#RRGGBB):" TextWrapping="Wrap" />
        <TextBox x:Name="ColorBox" Grid.Row="1" Margin="0,10,0,0" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK" Width="72" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click" />
            <Button Content="Cancel" Width="72" IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
